import { NavLink } from "react-router-dom";

const Navigation = () => {
  return (
    <div>
      <h1>
        <NavLink to="/">Home</NavLink>
      </h1>
    </div>
  );
};

export default Navigation;
